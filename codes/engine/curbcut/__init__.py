"""CurbCut deterministic accessibility engine.

The engine measures. It never decides what a finding means; IBM Bob does that
in the cc-review mode, and every "proven" finding must cite a fact_id from here.
"""

__version__ = "0.1.0"
AXE_CORE_VERSION = "4.13.0"
FIXTURE_ROOT = "codes/fixture/src"
